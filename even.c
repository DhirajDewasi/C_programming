#include<stdio.h>
int main()
{
    int n,i,even;
    printf("Enter a number:");
    scanf("%d",&n);
 for(i=1;i<=n;i++){
 even=2*i;
 }
    printf("Number from 1 is %d and even is %d",n,even);
    return 0;
}